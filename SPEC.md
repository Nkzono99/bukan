# Bukanの構成と境界

BukanはCLI、文献管理MCP、研究MCP、Codexプラグインから利用する。研究記録は外部ワークスペースに保存し、利用者はVS CodeのMarkdownプレビューとPDFビューアーで結果を確認する。Bukan専用GUIは廃止した。

```text
Codex / CLI
├─ Bukan文献管理MCP → Paperpile同期先（read-only）
├─ Bukan研究MCP → 研究ワークスペースのSQLite
└─ 作業別SKILL → 読解・監査・統合・表示確認

VS Code → 図表付きMarkdownと研究ログ
PDFビューアー → 原論文のページ・図表・数式
```

## 同じワークスペースを明示して使う

公開CLIは`bukan`とし、文献検出・索引・パス検査・ワークスペースの初期化をRustコアで共有する。

| 操作 | 契約 |
| --- | --- |
| `setup [workspace] [--default]` | 研究実行環境を準備する。接続先が未設定なら管理領域にワークスペースを作る。DBがなければ初期化し、既存形式1は自動移行しない |
| `paths --json` | `dataDir`、`configDir`、`cacheDir`、`managedWorkspace`、`defaultWorkspace`を表示する |
| `research [workspace] -- <engine args>` | 解決したワークスペースの研究DBでPythonエンジンを実行する |
| `mcp [workspace]` | 文献管理のstdio MCPを起動する |
| `research-mcp [workspace]` | 同じ研究DBを使うstdio MCPを起動する |
| `mcp-config [workspace] [--format json\|toml]` | 上記2サーバーの設定を標準出力へ出す。クライアント設定は変更しない |
| `init`, `detect`, `doctor`, `scan`, `organize` | 既存の文献管理・診断・分類案生成を継続する |

ワークスペースの選択は、コマンドの明示パス、`BUKAN_WORKSPACE`、Bukan専用の既定設定の順にする。`setup`は未設定時だけ管理ワークスペースを作成する。明示したパスや保存済みの設定が不正なら停止し、別の研究へフォールバックしない。カレントディレクトリから別の研究を推測しない。Bukanの既定設定とCodexのグローバル設定は別に扱う。

新規の管理ワークスペースはWindowsでは`%LOCALAPPDATA%/bukan/workspaces/default`、Linuxでは`$XDG_DATA_HOME/bukan/workspaces/default`または`~/.local/share/bukan/workspaces/default`に置く。絶対パスの`BUKAN_DATA_DIR`はBukanのデータ領域を変更し、既存ワークスペースを移動する指定にはしない。設定と再生成可能な実行環境キャッシュは分けて保存する。

既存の研究DB・安定ID・改訂・ノート・wiki・図表を保持する。GUIからの移行に新しい研究フォルダへのコピーは不要である。旧アプリがAppDataへ作った管理ワークスペースも、既存の`bukan.toml`を指定して使う。手順は[利用ガイド](docs/usage.md)を参照。

## Paperpileの原本を変更しない

Paperpileは受け入れ済みの文献・PDF・引用情報の正本とし、同期先は常に読み取り専用にする。自動出力された`data/paperpile.bib`も編集しない。ファイル名変更、移動、削除、ラベルの反映を同期フォルダ経由で行わない。

文献IDは書誌ファイル名とサイズに基づく`p2-...`形式を使い、同じ文献のコレクション間移動を追跡する。旧パス由来IDは互換参照として保持する。索引化ではPDF本文を開かず、Driveのオンデマンド実体化を必要としない。本文・画像の取得時には、実際のPDFのSHA-256と総ページ数を照合する。

候補は`candidates/`、承認済みのインポート用ファイルは`imports/`、分類案は`reports/`へ保存する。候補の保存はPaperpileへの登録・購入・変更を許可するものではない。`data/collections.json`の独自分類もPaperpileへ書き戻さない。

## 研究記録と人間の編集を保つ

研究エンジンは`data/research.sqlite`に文献、Source、Evidence、Claim、Relation、Question、Topic、PaperNote、作業、wikiを保存する。CLIとMCPは同じエンジンを使う。JSON窓口は`request`を標準名とし、旧`desktop`名は互換用の別名として残す。

更新は読んだ改訂を`expected_revision`に指定し、競合したら現行内容を読み直して統合する。SourceとEvidenceは固定記録であり、訂正は新しいIDと参照で表す。科学的な根拠への参照はIDと改訂を固定し、案内用リンクは現在版へ接続する。

新規DBは形式2にする。形式1は参照でき、書き込み前に明示的な`migrate`を行う。移行はSQLiteバックアップを作り、既存ID・本文・改訂を保持する。通常の参照や`setup`で既存DBの形式を変えない。

Markdown出力は閲覧・共有用のスナップショットである。手修正を自動で上書きせず、DBへ取り込むときは元の改訂と差分を確認する。ノートの図表とwikiの画像は、改訂に対応する永続資料として保持する。

## 全文読解・科学的点検・表示確認を分ける

全文レビューは参考文献・付録を含むPDF全ページを読み、図表、数式、キャプションを確認する。要旨や一部ページは選別用の暫定資料とする。未取得・未読・判読不能を明記し、取得や抽出の成功だけで読了にしない。

文献別の担当はホストが利用可能な範囲で並列実行する。モデルと推論設定は利用者の指定に従い、グローバル設定を変更しない。別担当が重要な本文記述と保存する全Claimの条件・根拠・計算を原PDFに照らして点検する。構造検査と原文抜粋の完全一致は、科学的な妥当性を保証しない。

wikiは複数論文を条件付きで比較した現在の解釈を持つ。追加サーベイは、引用・被引用の反復探索に加え、引用関係のない関連研究も独立に探す。全文読解ノートを保存した後、影響するwikiの節を改訂するか、比較した結果と変更不要の理由を残す。件数や予算の区切りを分野全体の調査完了にしない。

重要な図表はMarkdown画像として埋め込み、出典のPDF版・ページ・図表番号を付ける。表示数式は独立した`$$`ブロックを使う。持ち出し用Markdownには相対参照できる画像を文書配下へ同梱し、VS Codeなど指定された実際のプレビューで保存後に確認する。原式・原図はPDFビューアーで照合し、利用できない対象の確認は未検証として残す。

## 配布と保守

Cargo workspaceは`crates/bukan/`、研究エンジンは`research-engine/`に置く。配布物は実行ファイル、研究エンジン、必要なSKILLを含める。プラグイン定義は`plugins/bukan/`、研究指示の正本は`templates/workspace/`に置く。

Windowsはツール一式とプラグインの2段階で導入する。配布物の`install.cmd`から`install.ps1`を実行し、版と配布物ハッシュで分けたAppData内のインストール先へ配置する。固定版のuvとPoppler、研究用Python環境をユーザー領域へ準備し、管理者権限やシステムPATHの編集を前提にしない。個人マーケットプレイスの既存項目を保ってBukanを登録する。プラグインのインストール・有効化は2段階目として利用者がCodexで行う。Linuxは手動の依存導入とPythonインストーラーを継続する。

新しいワークスペースは作業別SKILLを同梱する。既存のカスタム指示や個人SKILLは自動上書きせず、更新時に比較して統合する。保存形式を変える場合は後方互換を保つか形式番号を上げる。パス検査・初期化・分類・改訂の挙動はテストする。

研究エンジンはLLM、外部クローラー、常駐スケジューラーを実行しない。未取得台帳や探索ログはホストが管理する。Paperpile全体の全文Markdown化は別途の作業であり、この構成移行には含めない。
